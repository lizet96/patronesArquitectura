// src/services/oldPaymentService.js
class OldPaymentService {
    makePayment(amount) {
        console.log(`Pago procesado con el sistema antiguo: $${amount}`);
    }
}

module.exports = OldPaymentService;

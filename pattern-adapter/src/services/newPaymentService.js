// src/services/newPaymentService.js
class NewPaymentService {
    executePayment(amount) {
        console.log(`Pago procesado con el sistema nuevo: $${amount}`);
    }
}

module.exports = NewPaymentService;
